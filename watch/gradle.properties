org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# The Android Gradle Plugin supports JDK 17 to 21. If your default JAVA_HOME is
# newer, AGP will refuse to run rather than misbehave quietly. Point Gradle at a
# supported JDK here instead of changing JAVA_HOME for everything else:
#
#   org.gradle.java.home=/path/to/jdk-21
